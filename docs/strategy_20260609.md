# 2026-06-09 Public Blend Push Strategy

Context:

- Current best public score after this batch is `7.551` from `jaemin_sp45_fleongg_w060`.
- The four Ridge-SP structural submissions from the prior queue completed above 8 (`8.173` to `8.322`), so they did not improve on the drift/geosteering score.
- The five public-blend/discussion candidates submitted after the 2026-06-10 UTC reset all completed successfully.

## Public Signal

- Recent public notebook: `jaemin3404/rogii-sp45-fleongg-blend-v2`.
- Relevant discussion signal: the CV/LB correlation thread includes a report of `CV 8.5, LB 7.5`, but that comment does not include code.
- The JAEMIN code is the actionable public release: it blends a projected SP45 Ridge/PF output with fleongg's pretrained geosteering model.

## Submitted Candidates

1. `jaemin_sp45_fleongg_exact`
   - Exact private reproduction of the public JAEMIN blend.
   - Final output is `submission_sp45_fleongg_w0.55.csv`.
   - Formula: `0.55 * SP45_projection + 0.45 * fleongg_pretrained`.

2. `jaemin_sp45_fleongg_w060`
   - Same public code path, but chooses the emitted `w0.60` blend as final.
   - Formula: `0.60 * SP45_projection + 0.40 * fleongg_pretrained`.
   - Rationale: close controlled perturbation of the likely 7.5 public solution; visible-overlap diagnostics and component strength both favor slightly more SP45, while vector distance from exact remains small.

3. `yaroslav_sel15_forced_selector`
   - Private preflight of `yaroslavkholmirzayev/rogii-sel15-forced-selector`.
   - Profile: Ridge/PF artifact experiment, `w_r=0.30`, `N_p=500`, `S=128`, `sigma_0=4.5`, robust `U=TVT+Z-anchor` projection degree `6`.
   - Rationale: discussion/public-reference candidate that claims the `ridge_artifact_param-7.715` signal. It is close to the SP45 projection family but not a duplicate of the JAEMIN/fleongg blends.

4. `jaemin_sp45_fleongg_jy`
   - Consensus-gated JY dynamic blend already preflighted as `surajranganath17/rogii-sp45-fleongg-jy-consensus`.
   - Formula: base `0.55 * SP45 + 0.45 * fleongg`, with a small gated JY dynamic correction where SP45/fleongg disagreement is high and JY remains close to the base.
   - Rationale: genuinely different third-round signal while keeping the correction weight small.

5. `iaztec_ridge_artifact_param`
   - Private fixed copy of `iaztec/rogii-wellbore-geology-ridge-artifact-param`.
   - Profile: Ridge/PF artifact experiment, `w_r=0.32`, `N_p=500`, `S=128`, `sigma_0=4.5`, projection degree `5`.
   - Rationale: second discussion link requested by the user. Version 1 failed only because `koolbox` was missing; version 2 includes the Yaroslav-style resolver/shim and has a validated Kaggle output.

## Risk Notes

- The public JY dynamic model package ran, but the feature frame was missing 68 expected columns.
- Because the queue must have five candidates, the JY output is included as the fifth-family signal, but only through the small consensus-gated correction rather than the larger raw JY blend.

## Queue State

- The queue submitted all five kernels after validating their outputs.
- Validation ranges:
  - `jaemin_sp45_fleongg_exact`: `11592.947..12239.154`
  - `jaemin_sp45_fleongg_w060`: `11591.980..12239.339`
  - `yaroslav_sel15_forced_selector`: `11589.941..12239.652`
  - `jaemin_sp45_fleongg_jy`: `11592.525..12238.937`
  - `iaztec_ridge_artifact_param`: `11589.096..12239.426`
- Pairwise prediction-vector RMSE across the five ranges from `0.536` to `2.094`.

## Results

- `jaemin_sp45_fleongg_w060`: `7.551`
- `jaemin_sp45_fleongg_exact`: `7.609`
- `jaemin_sp45_fleongg_jy`: `7.672`
- `iaztec_ridge_artifact_param`: `7.822`
- `yaroslav_sel15_forced_selector`: `7.903`

## Lessons

- The clean SP45/fleongg blend is the leader; increasing SP45 from `0.55` to `0.60` helped.
- The next targeted sweep should test nearby SP45-heavy weights, especially `0.62-0.70`, before spending slots on more ridge-artifact variants.
- JY correction and standalone artifact/projection references are weaker than the clean blend on public LB.
- Visible-overlap validation is not reliable for close blend selection.
