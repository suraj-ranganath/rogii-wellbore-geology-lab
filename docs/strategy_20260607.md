# 2026-06-07 Strategy Notes

Context:

- Current best submission is `ravaghi ridge hidden-safe v3 w040` with public RMSE `7.906`.
- Downloaded public leaderboard snapshot on 2026-06-07 00:12 PDT places the team at rank 74. Public rank 1 is `5.986`, and the top 10 cutoff is `7.248`.
- Daily submission cap remains 5. Final private ranking will be based on the hidden 74% split, so the final plan must keep one robust candidate and one aggressive candidate for final selection.

## Discussion/PPT Findings

- Welcome thread `697416`: the visible/downloadable test data is only for inference plumbing; the true hidden test set replaces it during code submission and should have no overlap with train except typewells.
- Timeout thread `703867`: code that finishes quickly on the 3 visible fake wells can time out on the hidden rerun because the scoring run has about 200 real test wells. Hidden-compatible candidates need 200-well runtime estimates.
- Stochastic-score threads `701995` and `704663`: public test is fixed, but many PF/feature pipelines are stochastic. We should set seeds inside numba/PF code and average stochastic features inside a single run rather than resubmitting for luck.
- LB-trust thread `704273`: CV and LB can diverge by pipeline family. Small tuning within a fixed pipeline tends to track LB better than large cross-family changes. Use local fold-safe validation for structure, public LB only for cautious calibration.
- Typewell thread `704839`: one participant found 57 unique typewells representing 773 horizontals; test wells may reuse existing/pseudo typewells. Distance and orientation to the assigned typewell should be treated as a reliability/confidence feature.
- ML+physics thread `704800`: physical-model logic still matters on hidden test even without visible train/test overlap; static visible overlap tricks should be avoided.
- Lithology thread `704771`: direct lithology labels have not shown reliable gains for participants. Treat them as segmentation/visualization/teacher signals, not first-order features.
- MTP thread `699853`: the strongest new clue is the trajectory/formation-state relation: `dTVT ~= -dZ + offset/state`, with long flat-formation stretches and discrete offset/dip events. GR is weak as a full-path matcher but useful for sparse anchor/offset selection.
- Organizer PPT: horizontal GR before prediction start can be a better-resolution same-well reference than vertical typewell GR. Use the known prefix as an anchor/template source, combined with typewell and deeper TVT information.

## Current Prediction Geometry

Pairwise RMSE-vector distances on the current public hidden outputs:

- `w040` vs `w035`: `0.565`, close but `w040` is materially better.
- `w040` vs `sunny`: `2.032`, meaning `sunny -> w040` is a plausible extrapolation direction.
- `w040` vs `targetfree`: `1.865`, but public-score geometry gives unstable extreme extrapolations because scores are rounded and target-free is much worse.
- `w040` vs `formation`: `1.839`, a useful diversity source if gated by confidence.

Public-score geometry suggests conservative extrapolation from `sunny`/`w035` toward and slightly beyond `w040` may help public score, but large public-fit extrapolations are not private-safe.

## Next Candidate Families

1. `w040` robust anchor refresh
   - Keep the hidden-safe Ridge/PF runtime profile.
   - Make PF/random features deterministic across reruns.
   - Add 200-train-well runtime smoke before pushing.

2. Conservative public-calibrated extrapolation
   - Dynamic kernel computes the same components as `w040` and `sunny`, then submits `w040 + beta * (w040 - sunny)` for small `beta` such as `0.20-0.50`.
   - This is for public climb, not the default private-final pick unless local validation agrees.

3. Formation-state/dZ candidate
   - Model `dTVT + dZ` as a discrete offset/state sequence from test-safe features.
   - Train on synthetic PS splits from train horizontals.
   - Reconstruct TVT by cumulative sum from the last known `TVT_input`.
   - Use GR/typewell only to choose offsets or anchor points, not as the sole objective.

4. Same-well prefix anchor candidate
   - Build anchors from the known horizontal prefix GR projected onto TVT, per PPT guidance.
   - Search post-PS windows against prefix/typewell templates with NCC/phase correlation.
   - Apply corrections only where anchor confidence is high.

5. Gated ensemble candidate
   - Blend `w040`, formation-state, prefix-anchor, and Sunny/PF components using fold-safe NNLS/ridge.
   - Gate by per-well confidence: typewell distance/orientation, NCC max, PF likelihood spread, prefix length, tail length, and formation-state entropy.

## Submission Policy

- Do not submit static public-ID CSV kernels again; hidden rerun will replace the test set and they will fail or no-score.
- Do not spend all 5 daily submissions on small variants of the same model.
- For each daily window, queue:
  - 2 robust candidates,
  - 2 calibrated/aggressive variants,
  - 1 genuinely different structural candidate.
- For final private selection, likely choose:
  - one robust generalization candidate,
  - one aggressive public/top-score candidate only if it is not obviously overfit in local grouped validation.
