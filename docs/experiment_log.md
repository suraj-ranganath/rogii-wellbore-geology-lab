# Experiment Log

All commands use `uv` from the project root. Generated JSON artifacts stay under
`outputs/` and are ignored by git; this log records the interpretable result.

## 2026-06-04

| Run | Command | Scope | RMSE | Notes |
| --- | --- | --- | ---: | --- |
| Last-known prior | `uv run rogii eval-priors` | all 773 train wells | 15.91 | Strong null; predicts the last known `TVT_input` across the hidden tail. |
| Linear prior | `uv run rogii eval-priors` | all 773 train wells | 113.63 | Not viable; tail TVT is not a simple MD extrapolation. |
| Residual LGBM smoke | `uv run rogii cv-baseline --config configs/smoke.yaml --output outputs/smoke_cv.json` | first 30 train wells, 3 GroupKFold splits | 17.22 | Baseline residual target before alignment features. |
| + prefix-NCC smoke | `uv run rogii cv-baseline --config configs/smoke.yaml --output outputs/smoke_cv_prefix_ncc.json` | first 30 train wells, 3 GroupKFold splits | 17.07 | Adds target-free GR self-correlation against known prefix. |
| + prefix-NCC + beam smoke | `uv run rogii cv-baseline --config configs/smoke.yaml --output outputs/smoke_cv_prefix_ncc_beam.json` | first 30 train wells, 3 GroupKFold splits | 16.65 | Adds deterministic typewell GR beam paths and residual features. |
| Kaggle calibration candidate | `python kaggle/kernels/last_known_baseline/last_known_baseline.py` | hidden rerun target, local visible smoke only | 15.91 local CV prior | Self-contained code-submission candidate to validate Kaggle notebook plumbing. |
| Kaggle submission 53350074 | `uv run kaggle competitions submit rogii-wellbore-geology-prediction -k surajranganath17/rogii-last-known-baseline -v 1 -f submission.csv -m "last-known TVT calibration baseline"` | Kaggle code submission | 15.883 public | Kernel version 1 completed and wrote `submission.csv`; calibration confirmed expected weak baseline. |
| PF selector spread3 | `uv run kaggle competitions submit rogii-wellbore-geology-prediction -k surajranganath17/rogii-pf-selector-spread3 -v 1 -f submission.csv -m "pf selector spread3 public reference"` | Kaggle code submission | 8.781 public | Submission ref `53350555`; adapted public `needless090/lb8-781-rogii-sel15-spread3`, kernel version 1 completed in about 3.2 minutes and wrote a valid `submission.csv`. |
| Physical-noise PF | `uv run kaggle competitions submit rogii-wellbore-geology-prediction -k surajranganath17/rogii-physical-noise-pf -v 1 -f submission.csv -m "physical noise pf 64 seed spread3"` | Kaggle code submission | 8.777 public | Submission ref `53353647`; adapted public `aiwody/physical-model-less-overfitting-noise`, kernel version 1 completed in about 2 minutes and wrote a valid `submission.csv`. |
| Super-solution top3 | `uv run kaggle kernels push -p kaggle/kernels/super_solution_top3 -t 32400 --accelerator gpu` | Kaggle GPU kernel | error | Distinct tree/physics stack adapted from public `romantamrazov/rogii-super-solution-lb-top-3`; failed after ~2.0h when CatBoost rejected `subsample` with default Bayesian bootstrap. No submission made. |
| Sunny+v10 artifact blend | `uv run kaggle competitions submit rogii-wellbore-geology-prediction -k surajranganath17/rogii-sunny-v10-artifact-blend -v 1 -f submission.csv -m "sunny v10 artifact blend"` | Kaggle code submission | 8.421 public | Submission ref `53356985`; reconstructs the public `kojimar/rogii-physical-pf-signal-meets-artifact-stack` 0.80 Sunny physical/PF + 0.20 v10 artifact-stack blend without using its inaccessible helper dataset; best completed submission so far. |
| Target-free alignment gated | `uv run kaggle competitions submit rogii-wellbore-geology-prediction -k surajranganath17/rogii-strat-align-sidecar-v2 -v 1 -f submission.csv -m "target free alignment gated"` | Kaggle code submission | 10.626 public | Submission ref `53359947`; private script-kernel copy of public `pilkwang/rogii-eda-target-free-alignment-for-tvt`, using `full_stack_sel15_gated_model_gated` with same-well physical shortcut disabled. Worse than PF/Sunny on the public split, but its prediction vector may still be useful as an anti-correlated extrapolation direction. |
| Public train-TVT extrapolate | `uv run python kaggle/kernels/public_train_tvt_extrapolate/public_train_tvt_extrapolate.py` | local candidate generation | not submitted | Public-only calibrated candidate: `train_tvt + 0.1579362539101862 * (train_tvt - last_known_tvt)`. The coefficient is solved from completed public scores for physical-noise PF/train-TVT 8.777 and last-known 15.883; predicted public RMSE from that two-point model is about 8.586. |
| Public LB blend optimizer | `uv run python scripts/public_lb_blend_optimizer.py` | local candidate generation | not submitted | Writes score-calibrated pairwise blend/extrapolation candidates under `outputs/public_lb_blend_candidates/`; ready to rerun when Sunny+v10 and target-free public scores resolve. |
| Public well-shift probes | `uv run python scripts/public_lb_probe_tool.py make --base-score 8.781 --shift 10` | local probe generation | not submitted | Writes one plus-shift probe per public well so tomorrow's public submissions can infer mean per-well residuals and build a corrected public-only candidate. |
| Next-window 5-candidate queue | `uv run python scripts/queue_next_window_candidates.py --timeout-minutes 720 --poll-seconds 180` | queued Kaggle code submissions | pending | Pushes fixed super-solution v2 plus four public-calibrated candidate kernels, waits for the 2026-06-05 00:00 UTC reset, then submits up to five completed candidates. |
| Static public-kernel v2 fix | `uv run python scripts/materialize_public_static_kernels.py && uv run kaggle kernels push -p <static_kernel_dir>` | queued Kaggle code submissions | not submitted | Static public candidates now embed their compressed CSV payload directly in the code file because Kaggle did not stage adjacent payload files for script kernels. Static v2 outputs were downloaded from Kaggle and verified to exactly match the intended local blend CSVs. |
| Super-solution top3 v3 | `uv run kaggle kernels push -p kaggle/kernels/super_solution_top3 -t 32400 --accelerator gpu` | queued Kaggle GPU kernel | pending | Version 2 fixed CatBoost bootstrap but failed when Kaggle assigned a single P100 and CatBoost rejected `devices="0:1"` because GPU id 1 did not exist. Version 3 uses `devices="0"`. |
| Next-window submission batch | `uv run python scripts/queue_next_window_candidates.py --timeout-minutes 720 --poll-seconds 180` | Kaggle code submissions | mixed | Five submissions were sent after the 2026-06-05 00:00 UTC reset. Super-solution v3 is pending. Public train-TVT scored 15.883 because hidden rerun data did not expose train TVT for the scoring wells and the kernel fell back to last-known. The three static public-only submissions threw hidden-rerun exceptions because Kaggle reran them against a different hidden dataset; do not submit static embedded-public CSV kernels again in this code competition. |
| 2026-06-05 submission audit | `uv run kaggle competitions submissions -v -c rogii-wellbore-geology-prediction` | Kaggle submission history | best 8.421 public | Confirmed current best is Sunny+v10 `8.421`; super-solution v3 scored `10.150`; public train-TVT fallback scored `15.883`; three static public-ID submissions completed without scores due hidden-rerun exceptions. |
| Formation-datum dynamic sanity | `ROGII_INPUT_DIR=/Users/suraj/Documents/dsc204a-final-project/data/raw/rogii-wellbore-geology-prediction uv run python formation_datum_dynamic.py` | visible overlap smoke | 3.55 visible RMSE | Dynamic hidden-compatible rewrite of the public formation-datum invariant. The visible score is only a plumbing sanity check because those example wells overlap train, but the row/order/value validation passed locally and after Kaggle output download. |
| H-blend v2 private probe | `uv run kaggle kernels push -p kaggle/kernels/h_blend_v2_private` | Kaggle GPU kernel | failed | Excluded from queue: TabICL raised `CUDA error: no kernel image is available for execution on the device` on Kaggle P100, and the notebook produced only `demo_submission.csv`, not a valid `submission.csv`. |
| Aiden PF scale 2.703125 | `uv run kaggle kernels push -p kaggle/kernels/aiden_pf_scale2703125` | Kaggle CPU kernel | ready but deprioritized | Completed and wrote a valid `submission.csv`, but its prediction vector differs from plateauavg by only `0.0119` RMSE-vector distance, so it is too redundant for the five-slot reset queue. |
| Formation-datum sigma sweep | local import of `formation_datum_dynamic.py` over `BLEND_SIGMA` values | visible overlap smoke | best 0.52 visible RMSE at sigma 20000 | Because larger sigma trusts the formation invariant more, the final queue includes both the public-inspired sigma 5000 and a more aggressive sigma 20000 variant for diversity. |
| 2026-06-05 next-reset queue | `uv run python scripts/queue_20260605_candidates.py --timeout-minutes 2400 --poll-seconds 300` | queued Kaggle code submissions | queued | Five hidden-compatible candidates are pushed/queued for the next UTC reset: Aiden PF plateauavg, formation-datum sigma 5000, fixed public blend v2, formation-datum sigma 20000, and Sunny+v10 70/30. The monitor submits only completed kernels after the 5/day cap resets. |

## 2026-06-07

| Run | Command | Scope | RMSE | Notes |
| --- | --- | --- | ---: | --- |
| Hidden-safe Ridge v3 batch | `uv run python scripts/queue_20260606_hidden_safe_candidates.py --timeout-minutes 2400 --poll-seconds 300` | Kaggle code submissions | best 7.906 public | All five hidden-safe Ridge submissions completed and scored. Best was `ravaghi ridge hidden-safe v3 w040` at `7.906`, improving over Sunny+v10 `8.421`. Other scores: w035 `8.108`, w030 `8.187`, w020 `8.233`, w025 `8.439`. |
| New discussion/PPT strategy sweep | `uv run kaggle competitions topic-messages ...` and PPT XML extraction | research | n/a | Updated `docs/strategy_20260607.md`. Main actionable idea is `dTVT ~= -dZ + offset/state`, but naive train-hidden validation of tail-offset dZ was poor (`~45` global RMSE), so dZ/state is not used standalone. Prefix GR and formation signals are only used as small gated corrections. |
| 2026-06-07 five-candidate materialization | `uv run python scripts/materialize_20260607_candidates.py` | generated Kaggle kernels | preflight valid | Generated five hidden-compatible candidates from the proven `w040` base: PF40 robust seed average, selector070, selector080, prefix-gated, and formation+prefix-gated. All script kernels pass `py_compile`. |
| 2026-06-07 candidate preflight | `uv run kaggle kernels push -p kaggle/kernels/ridge_w040_*` then `uv run kaggle kernels output ...` | Kaggle visible-output runs | valid outputs | All five pushed as version 1 and produced valid `submission.csv` files. Vector distances from current `w040`: PF40 `1.326`, selector070 `2.166`, selector080 `2.277`, prefix gate `3.716`, formprefix gate `2.285`. Visible runtimes were about 5.6-7.2 minutes. |
| 2026-06-07 local candidate smoke | `uv run python scripts/smoke_20260607_candidates.py` | local validation of downloaded Kaggle outputs | pass | Added a reusable no-submit harness for metadata, `py_compile`, log markers, sample shape/order, finite/range checks, baseline distance, and pairwise diversity. Current pairwise RMSE minimum is `0.360` between selector070 and selector080; all candidates pass the default `0.25` threshold. |
| 2026-06-07 visible-overlap score check | `uv run python scripts/score_visible_overlap.py` | 3 downloadable test wells with train ground truth | `w040` 2.579 | The current Kaggle-best `ridge_w040` scores `2.579` locally versus `7.906` public LB. The visible-overlap score is not leaderboard-comparable: PF/physical score `0.005` locally but `~8.78` on Kaggle, and Spearman rank correlation across known submissions is only `0.260`. Use it only for output diagnostics, not candidate selection. |
| 2026-06-07 local train-tail CV gate | `uv run python scripts/local_tail_cv.py --max-wells 80 --include-lgbm --lgbm-estimators 120` | 80 train wells, hidden-tail CV | best 14.427 | Added a local scorer that hides/evaluates the train tail by well. On 80 wells, `lgbm_last_residual` scored `14.427`, `ridge_last_residual` `14.831`, `last_known` `15.079`, `ridge_z_residual` `15.482`, `linear_md` `69.701`, and `z_anchor` `129.110`. This is a relative method gate, not a public-LB emulator. |
| 2026-06-07 repeated local proxy | `uv run python scripts/local_tail_cv.py --max-wells 80 --folds 5 --repeats 3 --splitter stratified --include-lgbm --lgbm-estimators 80` | 80 train wells, repeated stratified hidden-tail CV | best 14.679 | Added repeated, stratified-by-well CV to reduce split luck. Aggregate scores: `lgbm_last_residual` `14.679`, `ridge_last_residual` `14.874`, `last_known` `15.079`, `ridge_z_residual` `15.522`, `lgbm_z_residual` `22.870`, `linear_md` `69.701`, `z_anchor` `129.110`. This is the recommended fast local promotion gate. |
| 2026-06-07 PF selector replay proxy | `uv run python scripts/local_pf_selector_cv.py --max-wells 80 --n-particles 128 --n-seeds 8` | 80 train wells, PF/beam/selector replay | selector 11.448 | Added a direct local replay for the successful PF selector family. Aggregate scores: selector `11.448`, PF scales `11.995-12.216`, PF mean `13.183`, beam `14.829`, last-known `15.079`. Family-level local-vs-Kaggle correlation over Ridge/PF/last-known is now strong: Pearson `0.992`, Spearman `0.949`, but only across a small set of method families. |
| 2026-06-07 Ridge final-blend proxy | `uv run python scripts/local_ridge_final_blend_cv.py --max-wells 80 --selector-particles 128 --selector-seeds 8` | 80 train wells, final Ridge/PF blend replay | best 8.866 | Added exact final-blend scoring for the current strongest Ridge family. The scorer reconstructs the Ridge meta OOF from Ravaghi artifact trainers at `10.417`, close to the notebook log `10.434`. On submitted weights, local ranking is `w040` `9.319`, `w035` `9.478`, `w030` `9.657`, `w025` `9.857`, `w020` `10.075`, with Spearman `0.900` against Kaggle public. Heavy grid suggests `0.70` Ridge weight (`8.866`) and `0.60`/`0.80` (`8.918`) are worth testing. |
| 2026-06-07 ds-serv6 GPU smoke | `bash scripts/run_ds_serv6_tail_cv.sh tailcv_gpu_smoke_20260607 --max-wells 10 --folds 2 --include-catboost --catboost-task-type GPU --catboost-devices 0 --catboost-iterations 5` | ds-serv6 GPU 0 smoke | best 14.205 | Verified SSH, CSV-only rsync, remote `uv` bootstrap, tmux launch, and CatBoost GPU training on ds-serv6. The tiny 10-well smoke matched local behavior; GPU warning only reflected existing memory use on the shared server. |
| 2026-06-07 first next-reset queue | launchd `com.suraj.rogii.nextwindowqueue` running `scripts/queue_20260607_candidates.py` | queued Kaggle code submissions | stopped | Stopped before reset after the final-blend proxy ranked selector070/selector080/prefix/formprefix below the proven `w040` weight. |
| 2026-06-07 heavy-Ridge replacement queue | launchd `com.suraj.rogii.heavyridgequeue` running `scripts/queue_20260607_heavy_ridge_candidates.py` | queued Kaggle code submissions | waiting | Five replacement kernels are pushed and waiting at daily cap `5/5`: Ridge final weights `0.42`, `0.45`, `0.50`, `0.60`, and `0.70`. The monitor validates output before submission after reset. |
| 2026-06-07 exact-80 dynamic Z-blend search | `uv run python scripts/search_ridge_blend_formulas.py --components outputs/local_ridge_final_blend_components_80_s32p256.parquet --output outputs/ridge_blend_formula_search_80_s32p256_ext.json` | cached exact-80 Ridge/PF selector components | best 8.436 | Dynamic row-wise blending by global `z` quantile strongly beats fixed heavy Ridge blends on the exact-80 proxy: `z_rowq12` `8.436`, `z_rowq11` `8.452`, `z_rowq13` `8.494`, versus fixed global `0.729` at `9.187`, `0.75` at `9.189`, and `0.70` at `9.191`. Two-dimensional gates did not improve on `z_rowq12`. Exact-40 remains a stability warning because it prefers low fixed Ridge weight and does not like Z-bin blending. |
| 2026-06-07 dynamic Z-blend materialization | `uv run python scripts/materialize_20260607_dynamic_blend_candidates.py` | generated Kaggle kernels, no submit | preflight valid | Generated five hidden-compatible candidates from the proven `w040` base: `ridge_w040_zq08`, `ridge_w040_zq11`, `ridge_w040_zq12`, `ridge_w040_zq12_shr1000`, and `ridge_w040_zq13`. Each writes `dynamic-blend-spec.json` with CV folds, deploy cuts, and weights. All five script kernels pass `py_compile`. |
| 2026-06-07 dynamic Z next-reset queue | launchd `com.suraj.rogii.dynamiczqueue` running `scripts/queue_20260607_dynamic_z_candidates.py` | pushed Kaggle kernels, waiting at cap | queued/validated | Replaced the fixed heavy-Ridge queue before it could submit after reset. All five dynamic-Z kernels were pushed as version 1 and their actual Kaggle outputs were downloaded/validated: each has 14,151 rows, sample-order IDs, finite predictions, and TVT ranges around `11588..12239`. The monitor is waiting at `5/5` submissions today and will submit after the next UTC reset in order: `zq12`, `zq11`, `zq12_shr1000`, `zq13`, `zq08`. |

Interpretation:

- The local train-tail gate is now good enough to reject obviously bad standalone ideas such as MD or dZ extrapolation before using Kaggle submissions.
- The visible-overlap downloadable test score should not drive candidate ranking; it rewards overlap-specific behavior that does not transfer to Kaggle public.
- Residual models should be trained relative to `last_known`; residualizing around naive `z_anchor` is consistently worse.
- Next serious push should prioritize a small number of dynamic Z-blend variants plus any heavy-Ridge results that actually beat `w040`, while keeping at least one conservative heavy fixed-weight candidate because the exact-40 proxy disagrees with the Z-bin result.

## 2026-06-08

| Run | Command | Scope | RMSE | Notes |
| --- | --- | --- | ---: | --- |
| Dynamic-Z submission audit | `uv run kaggle competitions submissions -c rogii-wellbore-geology-prediction` | Kaggle public LB | best 8.613 public | All five dynamic-Z submissions completed but underperformed the proven `w040` score of `7.906`: `zq12_shr1000` `8.613`, `zq12` `8.879`, `zq13` `9.023`, `zq08` `9.062`, and `zq11` `9.101`. The exact-80 proxy was overfit and is no longer a primary selector for tiny variants. |
| Public code/discussion sweep | `uv run kaggle kernels list ...` and `uv run kaggle competitions topics show ...` | Kaggle research | n/a | Fresh scan still identifies the public `[LB 7.776]rogii-ridge-sp`, `rogii-ridge-sp45-proj`, `ROGII drift geosteering (infer)`, and Ridge artifact parameter experiments as the strongest reproducible code references. Discussion threads reinforce that the winning direction is structural/geosteering: lateral GR self-correlation, typewell reuse, segment/fault/stretch logic, signed azimuth/dip, and `TVT + Z` style formation coordinates. |
| External domain sweep | web search for ROGII StarSteer/geosteering/domain references | research | n/a | ROGII product material and geosteering references emphasize stratigraphic/model-based steering, lateral gamma correlation to typewells, stretch/squeeze/fault operations, dynamic dip/target-line logic, and uncertainty-aware auto-steering. This supports projection/geosteering candidates over more leaderboard-only curve fitting. |
| 2026-06-08 structural materialization | `uv run python scripts/materialize_20260608_structural_candidates.py` | generated Kaggle kernels | preflight valid | Generated five next-reset candidates: `ridge_sp7776_exact`, `ridge_sp45_proj`, `ridge_sp7776_proj`, `ridge_sp7776_projdeg3`, and `drift_geosteering_infer`. Ridge-SP candidates use the public stronger selector profile: PF/ANCC particles `600`, final selector `500 x 128`, all beam configs, and `0.30 Ridge / 0.70 selector`; projection variants smooth `U = TVT + Z - anchor` per well with robust polynomial degrees 5 or 3. |
| 2026-06-08 structural preflight | `uv run kaggle kernels push -p <candidate>` then `uv run kaggle kernels output ...` | Kaggle visible-output runs | valid outputs | All five kernels were pushed as version 1 and their actual Kaggle outputs were downloaded and validated: 14,151 rows, sample-order IDs, finite predictions, and plausible ranges. Visible runs completed for `drift_geosteering_infer` in ~121s and the Ridge-SP variants in ~6-10 minutes on the 3 visible wells. |
| 2026-06-08 structural queue | launchd `com.suraj.rogii.structuralqueue` running `scripts/queue_20260608_structural_candidates.py` | queued Kaggle code submissions | waiting | Queue is running and waiting at the daily cap (`5/5`) with no submissions sent yet. It will revalidate outputs and submit after the next UTC reset in this order: `ridge_sp7776_exact`, `ridge_sp45_proj`, `ridge_sp7776_proj`, `ridge_sp7776_projdeg3`, `drift_geosteering_infer`. |

Interpretation:

- The best current public submission remains `ravaghi ridge hidden-safe v3 w040` at `7.906`; the dynamic-Z batch did not improve it.
- The next five are intentionally not another micro-sweep. They combine one high-confidence public `7.776` Ridge-SP reproduction, two public/domain projection variants, one lower-degree projection stress test, and one independent pretrained geosteering stack.
- Execution risk is materially lower than the failed static-public and GPU experiments because every candidate produced a valid Kaggle output before queueing, and all submissions are code submissions with the correct competition/dataset sources.

| Run | Command | Scope | RMSE | Notes |
| --- | --- | --- | ---: | --- |
| Structural queue status | `uv run kaggle competitions submissions -c rogii-wellbore-geology-prediction` | Kaggle public LB | best 7.858 public | The structural queue submitted all five candidates after reset. `drift geosteering pretrained structural` completed at `7.858`, improving over `w040` `7.906`. The four Ridge-SP structural submissions remained pending at the time of the next candidate materialization. |
| New public-code scan | `uv run kaggle kernels list ...` and `uv run kaggle competitions topics show ...` | Kaggle research | n/a | Recent public code scan found `jaemin3404/rogii-sp45-fleongg-blend-v2`, a high-vote SP45 projection + fleongg pretrained blend matching the newly discussed 7.5-ish public approach. The CV/LB discussion also contains a 2026-05-29 comment reporting `CV 8.5, LB 7.5` for a single NN model, but no code is attached to that comment. |
| JAEMIN exact materialization | `uv run python scripts/materialize_20260609_jaemin_candidates.py` | generated Kaggle kernels | preflight valid | Generated a private exact copy of `jaemin3404/rogii-sp45-fleongg-blend-v2`: SP45 projected output blended as `0.55 * SP45 + 0.45 * fleongg_pretrained`. Pushed as `surajranganath17/rogii-sp45-fleongg-blend-exact`, version 1. Kaggle output validated with 14,151 rows and TVT range `11592.947..12239.154`. |
| JAEMIN modified W060 | `uv run python scripts/materialize_20260609_jaemin_candidates.py` | generated Kaggle kernels | preflight valid | Generated the same exact public code path but selected its emitted `submission_sp45_fleongg_w0.60.csv` as final `submission.csv`, making a controlled SP45-heavy variant. Pushed as `surajranganath17/rogii-sp45-fleongg-w060`, version 1. Kaggle output validated with 14,151 rows and TVT range `11591.980..12239.339`. Vector RMSE versus exact is `0.536`, p95 absolute delta `1.122`. |
| JY dynamic experiment | public `jaemin3404/rogii-sp45-fleongg-jy-dynamic-v1` private preflight | rejected for queue | n/a | The JY-augmented kernel ran and produced a valid output, but its public model package reported 68 missing feature columns and visible-overlap diagnostics were worse than the exact family. It was not queued; the safer second submission is the exact-family `w0.60` variant. |
| Yaroslav SEL15 discussion reference | `uv run kaggle kernels push -p kaggle/kernels/yaroslav_sel15_forced_selector` | Kaggle GPU preflight | validated output | Private preflight of `yaroslavkholmirzayev/rogii-sel15-forced-selector`, profile `w_r=0.30`, `N_p=500`, `S=128`, `sigma_0=4.5`, projection degree `6`. Kernel completed and wrote a valid `submission.csv` with 14,151 rows and TVT range `11589.941..12239.652`. Pairwise vector RMSE is `1.449` vs JAEMIN exact and `0.729` vs prior `ridge_sp45_proj`. |
| JY consensus correction | `uv run kaggle kernels output surajranganath17/rogii-sp45-fleongg-jy-consensus` | Kaggle CPU preflight | validated output | Added back as the fifth-family signal after correcting the queue size. The raw JY model package reports 68 missing feature columns, so the queued version uses only a small consensus-gated JY correction on top of the `0.55 SP45 + 0.45 fleongg` base. Output validation passed with 14,151 rows and TVT range `11592.525..12238.937`. |
| iAztec ridge-artifact param fix | `uv run kaggle kernels push -p kaggle/kernels/iaztec_ridge_artifact_param` | Kaggle preflight | validated output | Version 1 failed immediately because `koolbox` was unavailable. Patched the private notebook with the Yaroslav `koolbox` resolver/shim and added the offline koolbox dataset source; version 2 completed and wrote a valid `submission.csv` with 14,151 rows and TVT range `11589.096..12239.426`. |
| 2026-06-09 five-candidate queue | launchd `com.suraj.rogii.jaeminqueue` running `scripts/queue_20260609_jaemin_candidates.py` | Kaggle public LB | best 7.551 public | Corrected queue submitted all five after the 2026-06-10 UTC reset. Scores: JAEMIN `w0.60` `7.551`, JAEMIN exact `7.609`, JY consensus `7.672`, iAztec ridge-artifact-param v2 `7.822`, Yaroslav SEL15/projection-D6 `7.903`. This improves the prior best `drift geosteering pretrained structural` score of `7.858` by `0.307`. |

Interpretation:

- The SP45/fleongg public blend family is the strongest current direction. Moving from the public exact `w0.55` to `w0.60` improved public RMSE from `7.609` to `7.551`, so the next controlled sweep should bracket slightly higher SP45 weights rather than return to ridge-only variants.
- Ridge-artifact/projection-only discussion references are useful components but not standalone leaders: iAztec v2 scored `7.822`, Yaroslav D6 scored `7.903`, and prior Ridge-SP projections were `8.17+`.
- The JY dynamic correction was directionally worse than the clean JAEMIN blend despite a small gate: `7.672` versus `7.551`. Missing JY features remain a real warning; do not spend more slots on JY unless the missing feature frame is fixed.
- Visible-overlap diagnostics did not select the better close variant: it slightly preferred `w0.55`, while Kaggle public preferred `w0.60`. Keep visible-overlap for schema/plumbing only.
